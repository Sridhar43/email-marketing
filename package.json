{
  "name": "marketing",
  "version": "1.0.0",
  "description": "Marketing App",
  "scripts": {
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix email",
    "dev": "concurrently -k \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}